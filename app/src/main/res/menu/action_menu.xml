<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">
    <!-- "Mark Favorite", should appear as action button if possible -->
    <!--<item
        android:id="@+id/action_favorite"
        android:icon="@drawable/ic_form_black"
        android:title="black form"
        app:showAsAction="ifRoom"/>-->

    <!-- Settings, should always be in the overflow -->
    <item android:id="@+id/device_save"
    android:title="Save data to device"
    app:showAsAction="never"/>

    <item android:id="@+id/gmail_save"
        android:title="Attach data to Email"
        app:showAsAction="never"/>

    <item android:id="@+id/gdrive_save"
        android:title="Save data to drive"
        app:showAsAction="never"/>

</menu>
